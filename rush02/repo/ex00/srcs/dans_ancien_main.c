
/*
	int		fd;
    char    *str;
    int     **blocks;
    int     len_blocks;
    int     i;
    {
        len_blocks = ft_cpt_blocks(str);
        block = malloc((len_blocks + 1) * sizeof(int *));
        if (blocks == NULL)
            return(NULL);
        i = 0;
	    while (i < 3)
	    {
		    blocks[i] = malloc(3 * sizeof(int));
		    if (blocks[i] == NULL)
		    	return (NULL);
		    i++;
	    }
        ft_extract_int(str, blocks, len_blocks);
    }
    dictionary = fopen(name_dict, "r");
    if (dictionary == NULL)
        write(1, "Dict Open Error!\n", 17);
    else
    {

        
    }
	*/
